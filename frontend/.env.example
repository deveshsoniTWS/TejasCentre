# Dev server port (Vite)
PORT=3000

# Nest API base URL for /api proxy in dev
VITE_API_URL=http://localhost:8000

# Backend ENTRA_REDIRECT_URI must match this frontend callback URL, e.g.:
# ENTRA_REDIRECT_URI=http://localhost:3000/auth/entra/callback
